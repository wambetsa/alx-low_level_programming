#include "main.h"

/**
 * _strlen - The function is equal to strlen
 *
 * @s: my entry
 *
 * Return: the las character
 */
int _strlen(char *s)
{
	int a;

	for (a = 0; s[a] != '\0' ; a++)
	{
	}
	return (a);
}
