#include <stdio.h>
#include <stdarg.h>

/*
 *
 *
 *
 */
void print_numbers(const char *separator, const unsigned int n, ...)
{
	va_list ap;
	unsigned int i;

	va_start (ap, separator);
	for (i = 0; i < n; i ++)
	{
		
	}
}
